@model IEnumerable<WebPortal.CATCustomerData>

@{
    ViewBag.Title = "Index";
}

<ul class="nav nav-tabs" role="tablist">
    <li class="active">@Html.ActionLink(Resources.Labels.CustomerTab_Company, "CompanyList", "Customer")</li>
    <li>@Html.ActionLink(Resources.Labels.CustomerTab_Individual, "IndividualList", "Customer")</li>

</ul>


<h2>@Resources.Labels.CustomerTab_Company_PageTitle</h2>
<p>@Resources.Labels.CustomerTab_Company_PageSubtitle</p>


<div class="col-xs-12 col-sm-12 col-md-12">
    <div class="btn-group">
        <a class="modal-link btn btn-primary" href="/Customer/Create/1"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span> @Resources.Labels.Btn_Create </a>
    </div>
        <br />
    <br/>
    <div class="panel panel-primary">
        <div class="panel-heading">
            <h4 class="panel-title"><strong>@Resources.Labels.CustomerTab_Company_PageTitle</strong></h4>
        </div>
        <div>
            <input id ="SearchBox" name="Search" type="search"/>
            <button class="btn btn-danger" data-dismiss="modal" aria-hidden="true" onclick="location.href='@Url.Action("Search", "Customer")?Name=' + document.getElementById('SearchBox').value"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span> Hľadať </button>
        </div>
        <div class="panel-body">
            <table class="table table-striped table-bordered table-hover table-condensed">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyType)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyTAXID)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyVATID)
                    </th>
                   
                    <th>
                        @Html.DisplayNameFor(model => model.Address)                       
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ContactEmail)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ContactMobile)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ContactPhone)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyType)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyTAXID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyVATID)
                        </td>
                       
                        <td>
                            @Html.DisplayFor(modelItem => item.Address)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ContactEmail)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ContactMobile)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ContactPhone)
                        </td>
                        <td>
                            @*@Html.ActionLink("Edit", "Edit", new { id = item.PKCustomerDataID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.PKCustomerDataID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.PKCustomerDataID })*@
                            <div>
                                <a class="modal-link btn btn-success" href="/Customer/Edit/@item.PKCustomerDataID"><span class="glyphicon glyphicon-edit" aria-hidden="true"></span> @Resources.Labels.Btn_Edit </a>
                                <a class="modal-link btn btn-danger" href="/Customer/Delete/@item.PKCustomerDataID"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span> @Resources.Labels.Btn_Delete </a>
                            </div>
                        </td>
                    </tr>
                }

            </table>
            </div>
        </div>
    </div>


<div class="modal fade" id="modal-container" data-backdrop="static" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
        </div>
    </div>
</div>

<script type="text/javascript">
    //modal window
    $(function () {

        // Initialize modal dialog
        // attach modal-container bootstrap attributes to links with .modal-link class.
        // when a link is clicked with these attributes, bootstrap will display the href content in a modal dialog.
        $('body').on('click', '.modal-link', function (e) {
            e.preventDefault();
            $(this).attr('data-target', '#modal-container');
            $(this).attr('data-toggle', 'modal');
        });

        // Attach listener to .modal-close-btn's so that when the button is pressed the modal dialog disappears
        $('body').on('click', '.modal-close-btn', function () {
            $('#modal-container').modal('hide');
        });
        //clear modal cache, so that new content can be loaded
        $('#modal-container').on('hidden.bs.modal', function () {
            $(this).removeData('bs.modal');
        });
        $('#CancelModal').on('click', function () {
            return false;
        });
    });

</script>